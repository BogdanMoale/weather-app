/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "openweathermap.org",
      "t4.ftcdn.net",
      "encrypted-tbn0.gstatic.com",
      "images.stockcake.com",
    ],
  },
};

export default nextConfig;
